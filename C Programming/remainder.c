#include <stdio.h>
 
int main(void) {
//modulo operator
 
 
//devidend =devisor*quotient+remainder;
//like 14=4*2+2;
//points to be kept in mind
 
 
//remainder tells the distance from the nearest multiple like eg 13%4=1 means that its distance from nearest multiple ie 12 ie 1.
 
 
//if we devide 14%0 then it will give error 
// modulo of negative number is little bit shaky we can come up with remainder by removing the -ve sign.
 
//modulo of decimal or floating point is not possible as quotient sometimes become infinity repeating
 
 
 
 
int a=-14%3;
printf("%d",a);
 
 
 
 
 
 
 
 
 
 
 
 
 
 
	return 0;
}
 
